---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead
			/* Removes “Astro Blog” from the tab title */
			title="Bridgeit Advisory"
			description="Bridging research and ideas into real AI products."
		/>
	</head>

	<body class="home">
		<Header />

		<main class="deck">
			<!-- SLIDE 1 -->
			<section class="slide hero" aria-label="Bridgeit Advisory">
				<div class="heroOverlay" aria-hidden="true"></div>

				<div class="inner heroInner">
					<h1 class="heroTitle">Bridgeit Advisory</h1>
					<p class="tagline">Bridging research and ideas into real AI products</p>

					<div class="actions heroActions">
						<a class="btn primary" href="#contact">Contact</a>
						<a class="btn ghost" href="#what">What We Do</a>
					</div>
				</div>
			</section>

			<!-- SLIDE 2 -->
			<section class="slide s2" id="what">
			  <div class="inner s2Inner">
			    <div class="s2Layout">
			      <!-- LEFT -->
			      <div class="s2Left">
			        <p class="s2Kicker">What we do</p>

			        <h2 class="s2Headline">
			          Strategic, founder-led AI and
			          <span class="nowrap">research-to-product</span> advisory.
			        </h2>

			        <p class="s2Body">
			          We help founders, freelancers, and researchers turn complex ideas, research, and early demos into credible AI products
			          and industry-ready systems.
			        </p>

			        <div class="s2Chips" aria-label="Focus areas">
			          <span class="s2Chip">Product direction</span>
			          <span class="s2Chip">AI architecture</span>
			          <span class="s2Chip">MVP scoping</span>
			          <span class="s2Chip">Evaluation strategy</span>
			          <span class="s2Chip">Risk review</span>
			        </div>
			      </div>

			      <!-- DIVIDER -->
			      <div class="s2Divider" aria-hidden="true"></div>

			      <!-- RIGHT -->
			      <aside class="s2FocusCard" aria-label="Focus">
			        <p class="s2FocusLabel">Focus</p>
			        <p class="s2FocusText">
			          Our focus is clarity, direction, and decision-making, not outsourced execution.
			        </p>
			      </aside>
			    </div>
			  </div>
			</section>

			<!-- SLIDE 3 (REDESIGNED) -->
			<section class="slide s3" aria-label="How We Help">
				<div class="inner s3Inner">
					<div class="s3Layout">
						<!-- LEFT: framing -->
						<div class="s3Left">
							<p class="s3Kicker">How we help</p>
							<h2 class="s3Headline">Practical help that turns momentum into a plan.</h2>
							<p class="s3Body">
								We work with clients on the decisions that shape what to build, how to validate it, and what risks to avoid.
							</p>
							<p class="s3Note">
								The goal is to avoid building the wrong thing and move faster with confidence.
							</p>
						</div>

						<!-- RIGHT: cards -->
						<div class="s3Right" aria-label="How we help areas">
							<div class="s3Cards">
								<div class="s3Card">
									<p class="s3CardTitle">Product direction</p>
									<p class="s3CardText">Translate research or demos into a concrete product path and first validations.</p>
								</div>

								<div class="s3Card">
									<p class="s3CardTitle">Architecture decisions</p>
									<p class="s3CardText">RAG vs fine-tune, data strategy, evaluation design, deployment constraints.</p>
								</div>

								<div class="s3Card">
									<p class="s3CardTitle">MVP scoping</p>
									<p class="s3CardText">What to build now, what to defer, what to discard — so you don’t overbuild.</p>
								</div>

								<div class="s3Card">
									<p class="s3CardTitle">Prototype review</p>
									<p class="s3CardText">Hands-on feedback on experiments, prompts, retrieval setup, and demo clarity.</p>
								</div>

								<div class="s3Card">
									<p class="s3CardTitle">Risk review</p>
									<p class="s3CardText">Surface technical, data, and evaluation risks early — before they become months.</p>
								</div>

								<div class="s3Card">
									<p class="s3CardTitle">Next steps</p>
									<p class="s3CardText">Leave with a focused 1–4 week plan you can execute immediately.</p>
								</div>
							</div>
						</div>
					</div>
				</div>
			</section>

			<!-- SLIDE 4 REMOVED -->

			<!-- SLIDE 5 (REDESIGNED: ONLY "WHO WE WORK WITH") -->
			<section class="slide s5" aria-label="Who We Work With">
				<div class="inner s5Inner">
					<div class="s5Layout">
						<div class="s5Left">
							<p class="s5Kicker">Fit</p>
							<h2 class="s5Headline">Who we work with</h2>
							<p class="s5Body">
								Best fit is early-stage teams and individuals who need clarity and strong technical judgment — not a delivery factory.
							</p>
						</div>

						<div class="s5Right">
							<div class="s5Card" aria-label="We work with">
								<p class="s5CardLabel">We work with</p>
								<ul class="s5List">
									<li>Startup founders building AI-driven products</li>
									<li>Freelancers and independent builders</li>
									<li>PhDs, postdocs, and MScs transitioning to industry</li>
									<li>Research-driven teams validating applied directions</li>
								</ul>
							</div>

							<p class="s5Note">If you’re unsure, send a short note — we’ll tell you quickly if it’s a fit.</p>
						</div>
					</div>
				</div>
			</section>

			<!-- SLIDE 6 (REDESIGNED) -->
			<section class="slide s6" id="contact" aria-label="Contact">
				<div class="inner contactInner">
					<div class="contactGrid">
						<div class="contactLeft">
							<h2>Contact</h2>
							<p class="lead">Exploratory conversations are available by referral or direct outreach.</p>

							<p class="emailLine">
								📧 <a href="mailto:contact@bridgeitadvisory.com">contact@bridgeitadvisory.com</a>
							</p>

							<p class="muted contactNote">
								Share a couple of lines about what you’re building and what you need clarity on.
							</p>
						</div>

						<form class="form contactCard" method="POST" action="https://formspree.io/f/REPLACE_ME" aria-label="Contact form">
							<div class="row">
								<label>
									Name
									<input name="name" type="text" autocomplete="name" required />
								</label>
								<label>
									Email
									<input name="email" type="email" autocomplete="email" required />
								</label>
							</div>

							<label>
								Message
								<textarea name="message" rows="7" required></textarea>
							</label>

							<div class="actions contactActions">
								<button class="btn primary" type="submit">Send</button>
								<a class="btn ghost" href="mailto:contact@bridgeitadvisory.com">Email instead</a>
							</div>
						</form>
					</div>
				</div>
			</section>
		</main>

		<Footer />

		<style>
			/* hide header on slide 1 (hero) */
			:global(body.home header.wrap) { display: none; }

			/* remove global link underlines + kill default blue */
			:global(a) { text-decoration: none; color: inherit; }
			:global(a:hover) { text-decoration: none; }

			:global(html) { scroll-behavior: smooth; }
			:global(body) {
				margin: 0;
				overflow: hidden;
				background: #0b0f17;
				color: #eaf0ff;
			}

			.deck {
				height: 100vh;
				width: 100vw;
				overflow-y: auto;
				scroll-snap-type: y mandatory;
				scroll-snap-stop: always;
				-webkit-overflow-scrolling: touch;
			}

			.slide {
				min-height: 100vh;
				width: 100vw;
				scroll-snap-align: start;
				position: relative;
				display: grid;
				align-items: center;
				padding: 64px 48px;
			}

			.inner {
				width: 100%;
				max-width: 1400px;
				margin: 0;
				position: relative;
			}

			/* HERO slide */
			.hero {
				background: url('/hero-bridge.jpg') center / cover no-repeat;
				color: #ffffff;
			}

			/* lighter overlay so the photo shows through */
			.heroOverlay {
				position: absolute;
				inset: 0;
				background:
					linear-gradient(90deg, rgba(0,0,0,0.06) 0%, rgba(0,0,0,0.18) 45%, rgba(0,0,0,0.30) 100%),
					radial-gradient(900px 520px at 18% 25%, rgba(255,214,160,0.25), transparent 60%),
					linear-gradient(180deg, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.22) 70%, rgba(0,0,0,0.30) 100%);
				pointer-events: none;
			}

			.heroInner {
				position: relative;
				margin-left: 0;
				max-width: 1200px;
			}

			/* cooler “white” for the hero title */
			.heroTitle {
				font-size: clamp(72px, 8.5vw, 128px);
				font-weight: 700;
				letter-spacing: -0.04em;
				margin: 0 0 18px;
				color: #eaf0ff; /* cool white */
				text-shadow: 0 18px 50px rgba(0,0,0,0.55);
			}

			.tagline {
				font-size: clamp(22px, 3vw, 34px);
				max-width: 70ch;
				margin: 0;
				color: rgba(255,255,255,0.92);
				text-shadow: 0 12px 30px rgba(0,0,0,0.45);
			}

			/* Buttons: bigger text + perfect centering + no underline */
			.actions { display: flex; gap: 16px; margin-top: 28px; }

			.btn {
			  min-height: 60px;
			  padding: 0 28px;
			  font-size: 18px;
			  border-radius: 999px;

			  display: inline-flex;
			  align-items: center;
			  justify-content: center;

			  color: rgba(255,255,255,0.92);
			  text-decoration: none;
			  line-height: 1;
			}

			.primary {
			  background: rgba(255,214,160,0.25);
			  border: 1px solid rgba(255,214,160,0.6);
			  color: rgba(255,255,255,0.95);
			}

			.ghost {
			  background: rgba(255,255,255,0.15);
			  border: 1px solid rgba(255,255,255,0.25);
			  color: rgba(255,255,255,0.92);
			}

			/* ===== Base typography ===== */
			p { margin: 0 0 14px; }
			.lead { font-size: 18px; opacity: 0.92; max-width: 90ch; }
			.muted { opacity: 0.78; max-width: 92ch; }

			h2 {
				font-size: clamp(26px, 3vw, 38px);
				letter-spacing: -0.02em;
				margin: 0 0 14px;
			}
			h3 {
				font-size: 12px;
				letter-spacing: 0.14em;
				text-transform: uppercase;
				margin: 0 0 12px;
				opacity: 0.78;
			}

			/* ===== Slide 2 (kept) ===== */
			.s2 {
			  background:
			    radial-gradient(1100px 700px at 18% 20%, rgba(255,214,160,0.35), transparent 55%),
			    linear-gradient(180deg, #f6f7fb 0%, #eef1f9 100%) !important;
			  color: #0c1220 !important;
			}
			.s2 *,
			.s2 h2,
			.s2 p,
			.s2 li {
			  color: #0c1220 !important;
			}

			.s2 .kicker,
			.s2Kicker {
			  font-size: 12px;
			  letter-spacing: 0.16em;
			  text-transform: uppercase;
			  opacity: 0.7;
			  margin-bottom: 14px;
			}

			.s2 h2 {
			  font-size: clamp(44px, 5vw, 64px);
			  line-height: 1.05;
			  letter-spacing: -0.03em;
			  margin-bottom: 18px;
			}

			.s2 .lead {
			  font-size: 20px;
			  max-width: 80ch;
			  opacity: 0.95;
			}

			.s2 p {
			  max-width: 82ch;
			  opacity: 0.9;
			}

			.s2FocusCard {
			  background: rgba(255,255,255,0.92);
			  border: 1px solid rgba(12,18,32,0.10);
			  border-radius: 18px;
			  padding: 22px 26px;
			  box-shadow: 0 16px 40px rgba(12,18,32,0.12);
			}

			.s2Chip {
			  display: inline-flex;
			  padding: 8px 14px;
			  border-radius: 999px;
			  border: 1px solid rgba(12,18,32,0.14);
			  background: rgba(255,255,255,0.75);
			  color: #0c1220;
			  font-size: 13px;
			  opacity: 0.95;
			}

			.s2Divider {
			  width: 1px;
			  background: linear-gradient(
			    to bottom,
			    rgba(12,18,32,0.00),
			    rgba(12,18,32,0.18),
			    rgba(12,18,32,0.00)
			  );
			}

			/* ===== Slide 3 (readable) ===== */
			.s3 {
				background:
					radial-gradient(900px 520px at 20% 15%, rgba(255,214,160,0.14), transparent 60%),
					linear-gradient(180deg, #0b1020 0%, #070a14 100%);
			}

			.s3Inner { max-width: 1400px; }

			.s3Layout {
				display: grid;
				grid-template-columns: 1.05fr 1.15fr;
				gap: 28px;
				align-items: start;
			}

			.s3Kicker {
				font-size: 12px;
				letter-spacing: 0.16em;
				text-transform: uppercase;
				opacity: 0.75;
				margin: 0 0 14px;
			}

			.s3Headline {
				font-size: clamp(40px, 4.8vw, 64px);
				line-height: 1.06;
				letter-spacing: -0.03em;
				margin: 0 0 16px;
				color: rgba(255,255,255,0.96);
				text-shadow: 0 18px 50px rgba(0,0,0,0.45);
			}

			.s3Body {
				font-size: 18px;
				line-height: 1.6;
				max-width: 70ch;
				color: rgba(255,255,255,0.86);
				margin: 0 0 18px;
			}

			.s3Note {
				font-size: 16px;
				color: rgba(255,255,255,0.72);
				margin: 0;
			}

			.s3Cards {
				display: grid;
				grid-template-columns: repeat(2, minmax(0, 1fr));
				gap: 14px;
			}

			.s3Card {
				border: 1px solid rgba(255,255,255,0.12);
				background: rgba(255,255,255,0.06);
				border-radius: 18px;
				padding: 18px;
				backdrop-filter: blur(10px);
				-webkit-backdrop-filter: blur(10px);
				box-shadow: 0 18px 50px rgba(0,0,0,0.28);
			}

			.s3CardTitle {
				margin: 0 0 10px;
				font-size: 12px;
				letter-spacing: 0.14em;
				text-transform: uppercase;
				color: rgba(255,255,255,0.88);
			}

			.s3CardText {
				margin: 0;
				font-size: 15px;
				line-height: 1.55;
				color: rgba(255,255,255,0.78);
			}

			/* ===== Slide 5 (readable, single bubble) ===== */
			.s5 {
				background:
					radial-gradient(1000px 620px at 18% 18%, rgba(255,214,160,0.16), transparent 55%),
					linear-gradient(180deg, #0f141b 0%, #0a0d12 100%);
			}

			.s5Layout {
				display: grid;
				grid-template-columns: 1fr 1.1fr;
				gap: 28px;
				align-items: start;
			}

			.s5Kicker {
				font-size: 12px;
				letter-spacing: 0.16em;
				text-transform: uppercase;
				opacity: 0.75;
				margin: 0 0 14px;
				color: rgba(255,255,255,0.78);
			}

			.s5Headline {
				font-size: clamp(40px, 4.6vw, 62px);
				line-height: 1.06;
				letter-spacing: -0.03em;
				margin: 0 0 16px;
				color: rgba(255,255,255,0.96);
				text-shadow: 0 18px 50px rgba(0,0,0,0.45);
			}

			.s5Body {
				font-size: 18px;
				line-height: 1.6;
				max-width: 72ch;
				color: rgba(255,255,255,0.84);
				margin: 0;
			}

			.s5Card {
				border: 1px solid rgba(255,255,255,0.14);
				background: rgba(255,255,255,0.07);
				border-radius: 20px;
				padding: 22px;
				backdrop-filter: blur(12px);
				-webkit-backdrop-filter: blur(12px);
				box-shadow: 0 22px 70px rgba(0,0,0,0.30);
			}

			.s5CardLabel {
				margin: 0 0 12px;
				font-size: 12px;
				letter-spacing: 0.14em;
				text-transform: uppercase;
				color: rgba(255,255,255,0.80);
			}

			.s5List {
				margin: 0;
				padding-left: 18px;
				color: rgba(255,255,255,0.88);
			}
			.s5List li {
				margin: 10px 0;
				line-height: 1.45;
				color: rgba(255,255,255,0.86);
			}

			.s5Note {
				margin-top: 14px;
				color: rgba(255,255,255,0.72);
				font-size: 14px;
			}

			/* ===== Slide 6 (redesigned contact) ===== */
			.s6 {
				background:
					radial-gradient(1100px 700px at 18% 20%, rgba(255,214,160,0.18), transparent 55%),
					linear-gradient(180deg, #0b1020 0%, #070a14 100%);
			}

			.s6 h2 {
			  color: rgba(255,255,255,0.96) !important;
			  text-shadow: 0 18px 50px rgba(0,0,0,0.45);
			}
			.contactInner { max-width: 1400px; }

			.contactGrid {
				display: grid;
				grid-template-columns: 1fr 1.15fr;
				gap: 28px;
				align-items: start;
			}

			.emailLine { margin: 10px 0 16px; }
			.emailLine a {
				color: rgba(255,255,255,0.92);
				font-weight: 600;
			}

			.contactNote { margin-top: 8px; }

			/* Form as a readable “bubble” */
			.contactCard {
				border: 1px solid rgba(255,255,255,0.14);
				background: rgba(255,255,255,0.06);
				border-radius: 20px;
				padding: 22px;
				backdrop-filter: blur(12px);
				-webkit-backdrop-filter: blur(12px);
				box-shadow: 0 22px 70px rgba(0,0,0,0.30);
			}

			.form { margin-top: 0; max-width: none; padding-top: 0; border-top: none; }

			.row { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }

			label {
				display: grid;
				gap: 8px;
				font-size: 13px;
				color: rgba(255,255,255,0.82);
				margin-bottom: 12px;
			}

			input, textarea {
				width: 100%;
				padding: 12px 14px;
				border-radius: 14px;
				border: 1px solid rgba(255,255,255,0.18);
				background: rgba(0,0,0,0.18);
				color: #eaf0ff;
				outline: none;
			}

			input:focus, textarea:focus {
				border-color: rgba(255, 214, 160, 0.55);
				box-shadow: 0 0 0 4px rgba(255, 214, 160, 0.18);
			}

			.contactActions { margin-top: 16px; }

			@media (max-width: 980px) {
				.row { grid-template-columns: 1fr; }
				.heroTitle { font-size: clamp(52px, 10vw, 82px); }
				.btn { min-height: 58px; font-size: 16px; padding: 0 26px; }

				.s3Layout { grid-template-columns: 1fr; }
				.s3Cards { grid-template-columns: 1fr; }

				.s5Layout { grid-template-columns: 1fr; }

				.contactGrid { grid-template-columns: 1fr; }
			}

			@media (prefers-reduced-motion: reduce) {
				:global(html) { scroll-behavior: auto; }
			}
		</style>
	</body>
</html>
